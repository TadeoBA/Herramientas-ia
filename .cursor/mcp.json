{
    "mcpServers": {
      "supabase": {
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-postgres",
                 "postgresql://postgres:postgres@127.0.0.1:54322/postgres"],
        "disabled": false,
        "description": "Conexión directa a PostgreSQL local — auditoría de contratos y reglas de cumplimiento FinGuard-AI"
      }
    }
  }